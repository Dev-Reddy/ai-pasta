"use client";
import { Info, KeyRound, MessageSquare, Layers3, FolderKanban } from "lucide-react";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";

export function InfoDialog({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) {
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="sm:max-w-[620px]"><DialogHeader><DialogTitle className="flex items-center gap-2"><Info className="h-5 w-5 text-purple-400" /> How AI Pasta works</DialogTitle><DialogDescription>One workspace for comparing answers from multiple AI providers.</DialogDescription></DialogHeader><div className="grid gap-4 sm:grid-cols-2"><div className="rounded-lg border p-4"><KeyRound className="mb-2 h-5 w-5 text-purple-500" /><h3 className="font-medium">1. Add your keys</h3><p className="mt-1 text-sm text-muted-foreground">Add provider keys in Settings. They stay in this browser&apos;s local storage.</p></div><div className="rounded-lg border p-4"><Layers3 className="mb-2 h-5 w-5 text-purple-500" /><h3 className="font-medium">2. Enable models</h3><p className="mt-1 text-sm text-muted-foreground">Choose providers and select a model for each. Custom model IDs are supported.</p></div><div className="rounded-lg border p-4"><MessageSquare className="mb-2 h-5 w-5 text-purple-500" /><h3 className="font-medium">3. Compare responses</h3><p className="mt-1 text-sm text-muted-foreground">Send one prompt to all enabled models and compare their responses side by side.</p></div><div className="rounded-lg border p-4"><FolderKanban className="mb-2 h-5 w-5 text-purple-500" /><h3 className="font-medium">4. Add project context</h3><p className="mt-1 text-sm text-muted-foreground">Projects save a system context for reuse across conversations.</p></div></div><p className="text-xs text-muted-foreground">Keys are not stored on the app server. Keep your browser and extensions trusted.</p></DialogContent></Dialog>;
}
